import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.weightwave.app',
  appName: 'Weight Wave',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
