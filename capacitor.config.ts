import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.weightmate.ionic',
  appName: 'Weight Mate',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
