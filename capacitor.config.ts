import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'weight-mate-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
